{
  "name": "tippi",
  "version": "1.0.0",
  "description": "Tippi Game",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step needed' && exit 0",
    "android": "npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/core": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest"
  }
}